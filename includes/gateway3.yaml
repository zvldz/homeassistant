attributes_template: |
  {% if attr in ('ble', 'zigbee', 'battery') %}{{{
    "name": device["device_name"],
    "gate": gateway["device_name"],
    "fw_version": device["fw_ver"],
    "integration": "gw3"
  }}}{% endif %}

#gw3_command: "/data/gw3 -log=syslog,debug,btraw,btgap,miio 2>&1 | mosquitto_pub -t gw3/stderr -s &"
gw3_command: "/data/gw3 -log=syslog,debug,miio 2>&1 | mosquitto_pub -t gw3/stderr -s &"
