ha1_restart:
  url: "{{ states('input_text.ha1_url') }}api/services/homeassistant/restart"
  method: POST
  headers:
    authorization: !secret ha1_token

ha2_restart:
  url: "{{ states('input_text.ha2_url') }}api/services/homeassistant/restart"
  method: POST
  headers:
    authorization: !secret ha2_token

ha1_kitchen_lamp_reload:
  url: "{{ states('input_text.ha1_url') }}api/config/config_entries/entry/565e8dc97edebf9b8d757407873d7aee/reload"
  method: POST
  headers:
    authorization: !secret ha1_token

ha2_kitchen_lamp_reload:
  url: "{{ states('input_text.ha2_url') }}api/config/config_entries/entry/565e8dc97edebf9b8d757407873d7aee/reload"
  method: POST
  headers:
    authorization: !secret ha2_token

